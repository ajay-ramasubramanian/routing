{
  "name": "chiroatwater-routing-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for routing requests between chiroatwater and godot websites",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
